
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/swingforme/config2
          username: swingforme@126.com
          password: TEX362xf
          default-label: master
          search-paths: src/main/resources/pay,src/main/resources/online
      name:

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

